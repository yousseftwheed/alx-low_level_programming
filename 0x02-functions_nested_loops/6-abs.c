#include"main.h"

/**
*_abs - to check for abs value
*Description: task 6
*@x: passed number to be checked
*Return: x if +ve && x *-1 if -ve
*/

int _abs(int x)
{
if (x >= 0)
return (x);
else
return (x * -1);
}
