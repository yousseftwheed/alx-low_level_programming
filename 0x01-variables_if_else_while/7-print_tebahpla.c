#include<stdio.h>
/**
* main -entry point of programe
* Description : small program
* Return: 0 (Success)
*/

int main(void)
{
	char i;
for (i = 'z'; i >= 'a'; i--)
	putchar(i);
putchar('\n');
return (0);
}
